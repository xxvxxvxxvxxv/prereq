# Docker Compose reads this file when copied to .env. start.py does NOT need it.
# Replace with your actual HTTPS hostname before public deployment.
PREREQ_HOSTS=localhost,127.0.0.1
# PREREQ_OFFLINE=1
